# Анализ заглушек и неработающих кнопок в Komet

Этот документ содержит полный список UI элементов, которые являются заглушками или имитируют функциональность, которая еще не реализована.

## 1. Ограничение FPS (Оптимизация)

**Файл:** `lib/screens/settings/optimization_screen.dart`  
**Строки:** 53-65 (основной экран), 109-126 (модальное окно)

### Описание
ListTile со слайдером для "ограничения FPS"

### Текст в интерфейсе
- Заголовок: "Да ниче он бля не делает"
- Подзаголовок: "Оно визуал😭😭"

### Код
```dart
ListTile(
  contentPadding: EdgeInsets.zero,
  leading: const Icon(Icons.timeline),
  title: const Text("Да ниче он бля не делает"),
  subtitle: Text("Оно визуал😭😭"),
),
Slider(
  value: theme.maxFrameRate.toDouble(),
  min: 30,
  max: 120,
  divisions: 9,
  label: "${theme.maxFrameRate} FPS",
  onChanged: (value) {
    theme.setMaxFrameRate(value.round());
  },
),
```

### Статус
❌ **Заглушка** - Слайдер визуально работает и сохраняет значение, но не влияет на реальное ограничение частоты кадров приложения. Это чисто визуальный элемент без реальной функциональности.

---

## 2. Описание возможностей плагина

**Файл:** `lib/screens/settings/plugins_screen.dart`  
**Строка:** 532

### Описание
Текст, отображаемый когда у плагина нет описания возможностей

### Текст в интерфейсе
"Плагин хуетень ниче не делает "

### Код
```dart
if (summary.isEmpty)
  Text(
    'Плагин хуетень ниче не делает ',
    style: theme.textTheme.bodyMedium?.copyWith(
      color: theme.colorScheme.onSurface.withOpacity(0.5),
    ),
  )
```

### Статус
❌ **Заглушка** - Показывается когда у плагина отсутствует массив с описанием возможностей (summary). Это placeholder текст для пустого состояния.

---

## 3. Экран звонков

**Файл:** `lib/screens/chat/screens/calls_screen.dart`  
**Строки:** 1-13

### Описание
Полностью нереализованный экран для функционала звонков

### Текст в интерфейсе
"Звонки скоро будут доступны"

### Код
```dart
class CallsScreen extends StatelessWidget {
  const CallsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Звонки')),
      body: const Center(child: Text('Звонки скоро будут доступны')),
    );
  }
}
```

### Статус
❌ **Заглушка** - Целый экран является placeholder. Функционал звонков полностью не реализован. Экран содержит только AppBar и текст по центру.

---

## 4. Фишки с цветными никнеймами

**Файл:** `lib/screens/settings/bypass_screen.dart`  
**Строки:** 228-248

### Описание
Информационная секция о будущем функционале подсветки букв в никнеймах

### Текст в интерфейсе
- Заголовок: "Фишки (цветные никнеймы, скоро)"
- Описание: "В будущих версиях можно будет подсвечивать отдельные буквы и слова в нике с помощью простого синтаксиса, а также добавлять визуальные эффекты к сообщениям."

### Код
```dart
Row(
  children: [
    Icon(Icons.color_lens_outlined, color: colors.primary),
    const SizedBox(width: 8),
    Text(
      'Фишки (цветные никнеймы, скоро)',
      style: TextStyle(
        fontWeight: FontWeight.w600,
        color: colors.primary,
      ),
    ),
  ],
),
const SizedBox(height: 8),
Text(
  "В будущих версиях можно будет подсвечивать отдельные буквы и слова в нике с помощью простого синтаксиса, а также добавлять визуальные эффекты к сообщениям.",
  style: TextStyle(
    color: colors.onSurfaceVariant,
    fontSize: 14,
  ),
),
```

### Статус
❌ **Заглушка** - Информационный блок о функционале, который будет реализован в будущем. Сейчас это просто текстовое описание без какой-либо функциональности.

---

## 5. Режим ПК на мобильных устройствах

**Файл:** `lib/screens/settings/customization_screen.dart`  
**Строки:** 581-589

### Описание
Switch для включения режима ПК с предупреждением о неработоспособности на телефонах

### Текст в интерфейсе
- Заголовок: "Режим ПК"
- Подзаголовок: "Не работает на телефонах"

### Код
```dart
_CustomSettingTile(
  icon: Icons.desktop_windows,
  title: "Режим ПК",
  subtitle: "Не работает на телефонах",
  child: Switch(
    value: theme.useDesktopLayout,
    onChanged: (value) => theme.setUseDesktopLayout(value),
  ),
),
```

### Статус
⚠️ **Частично работает** - Switch работает и сохраняет значение, но функционал корректно работает только на десктопных платформах (Windows, Linux, macOS). На мобильных устройствах (Android, iOS) эта опция не имеет эффекта, что явно указано в подзаголовке.

---

## 6. Поле "О себе" в профиле

**Файл:** `lib/screens/manage_account_screen.dart`  
**Строки:** 288-297

### Описание
Многострочное текстовое поле для ввода описания профиля (биографии)

### Текст в интерфейсе
- Заголовок секции: "Дополнительно"
- Label поля: "О себе"

### Код
```dart
TextFormField(
  controller: _descriptionController,
  maxLines: 4,
  maxLength: 400,
  decoration: _buildInputDecoration(
    "О себе",
    Icons.edit_note_outlined,
    alignLabel: true,
  ),
),
```

### Логика отправки
```dart
// В методе _saveProfile():
final description = _descriptionController.text.trim();

final updatedProfile = await ApiService.instance.updateProfileText(
  firstName,
  lastName,
  description,
);

// В api_service_media.dart:
final Map<String, dynamic> payload = {
  "firstName": firstName,
  "lastName": lastName,
};
if (description.isNotEmpty) {
  payload["description"] = description;
}
```

### Статус
❌ **Заглушка** - Полная имитация работающего функционала:
- UI отображает поле ввода с валидацией (макс. 400 символов)
- Данные собираются и отправляются на сервер через API
- При успешном ответе показывается SnackBar "Профиль обновлен"
- **НО**: Сервер не обрабатывает и не сохраняет поле `description`
- Введенный текст теряется после "сохранения"
- При повторном открытии экрана поле всегда пустое

Это классический пример "закоса" - клиентская часть полностью реализована и создает иллюзию работающей функции, но серверная часть не имплементирована.

---

## Итого

**Всего найдено:** 6 элементов-заглушек

**Категории:**
- Полностью нереализованные (экран звонков) - 1
- Визуальные заглушки без функциональности (FPS слайдер) - 1
- Placeholder тексты (описание плагина) - 1
- Анонсы будущего функционала (цветные никнеймы) - 1
- Частично работающие элементы (режим ПК) - 1
- Имитация работающего функционала (поле "О себе") - 1

**Рекомендации:**
1. Рассмотреть возможность скрытия нереализованных элементов UI до момента их реализации
2. Заменить откровенные placeholder тексты на более информативные сообщения
3. Добавить четкие индикаторы для функционала "в разработке" (например, badge "СКОРО" или "BETA")
4. Для платформо-зависимых функций добавить автоматическое скрытие неработающих опций
5. Для имитированных функций (как "О себе") - либо удалить UI до реализации серверной части, либо добавить предупреждение о нерабочем статусе
